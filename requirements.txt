# Core TwitchDropsMiner dependencies
aiohttp>=3.9,<4.0
Pillow>=9.0.0
pystray>=0.19.0
PyGObject<3.51; sys_platform == "linux"  # required for better system tray support on Linux

# TwitchMultiMiner service dependencies
flask>=2.3.0
psutil>=5.9.0
requests>=2.31.0
cryptography>=41.0.0
jsonschema>=4.17.0

# Development and testing dependencies
# (These are included for completeness but may be moved to requirements-dev.txt)
# unittest is part of Python standard library
# pathlib is part of Python standard library
# json is part of Python standard library
# asyncio is part of Python standard library
# datetime is part of Python standard library
# typing is part of Python standard library

# Environment-dependent dependencies
pywin32>=306; sys_platform == "win32"
truststore>=0.8.0

# Optional GUI dependencies (for tkinter-based interface)
# tkinter is part of Python standard library on most systems
